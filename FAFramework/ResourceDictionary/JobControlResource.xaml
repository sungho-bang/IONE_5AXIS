<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Style TargetType="DockPanel">
        <Style.Setters>
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Top" />
        </Style.Setters>
    </Style>
    <Style TargetType="Label" BasedOn="{StaticResource ResourceKey=OnOffSensorLabel}">
        <Style.Setters>
            <Setter Property="Width" Value="25" />
            <Setter Property="Height" Value="25" />
        </Style.Setters>
    </Style>
    <Style TargetType="TextBlock" BasedOn="{StaticResource ResourceKey=baseTextBlockStyle}">
        <Style.Setters>
            <Setter Property="Margin" Value="2" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style.Setters>
    </Style>
    <Style TargetType="TextBox" BasedOn="{StaticResource ResourceKey=baseStyle}">
        <Style.Setters>
            <Setter Property="Margin" Value="2" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="MinWidth" Value="50" />
        </Style.Setters>
    </Style>
    <Style TargetType="GroupBox">
        <Setter Property="Margin" Value="5" />
        <Setter Property="Background" Value="DodgerBlue" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GroupBox}">
                    <Border Style="{StaticResource ResourceKey=borderBaseStyle}" Background="DodgerBlue">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ContentControl HorizontalContentAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="3">
                                <ContentPresenter ContentSource="Header" />
                            </ContentControl>
                            <Grid Grid.Row="1" Background="White">
                                <ContentPresenter />
                            </Grid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="Button" BasedOn="{StaticResource ResourceKey=baseStyle}">
        <Style.Setters>
            <Setter Property="Margin" Value="2" />
        </Style.Setters>
    </Style>
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source= "/FAFramework;component/ResourceDictionary/CustomStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>    
</ResourceDictionary>