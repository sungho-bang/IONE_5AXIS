<UserControl x:Class="FAFramework.GUI.Standard.ManualSubUnitDetailOperationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource self}}"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:converter="clr-namespace:FAFramework.Utility.Converter"
             xmlns:utility="clr-namespace:FAFramework.Utility">
    <Grid>
        <Grid>
            <ListView Height="Auto" HorizontalAlignment="Stretch" Margin="0" Name="listView1" VerticalAlignment="Stretch" Width="Auto" ItemsSource="{Binding PartDefineList.Values}"
                              ItemContainerStyle="{StaticResource alternatingStyle}" AlternationCount="2">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="{DynamicResource ResourceKey=Name}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Path=Name}" VerticalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="{DynamicResource ResourceKey=Status}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TreeView ItemsSource="{Binding Path=StatusObjectList}">
                                        <TreeView.Resources>
                                            <HierarchicalDataTemplate DataType="{x:Type utility:BooleanPropertyValue}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition />
                                                        <ColumnDefinition />
                                                        <ColumnDefinition />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Path=Name}" Grid.Column="0" />
                                                    <TextBlock Text=" : " Grid.Column="1" />
                                                    <CheckBox  IsChecked="{Binding Path=Value}" Grid.Column="2"/>
                                                </Grid>
                                            </HierarchicalDataTemplate>
                                            <HierarchicalDataTemplate DataType="{x:Type utility:ReadOnlyPropertyValue}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Path=Name}" Grid.Column="0" />
                                                    <TextBlock Text=" : " Grid.Column="1" />
                                                    <TextBlock Text="{Binding Path=Value}" Grid.Column="2" />
                                                </Grid>
                                            </HierarchicalDataTemplate>
                                            <HierarchicalDataTemplate DataType="{x:Type utility:PropertyContainer}" ItemsSource="{Binding Path=Value}">
                                                <TextBlock Text="{Binding Path=Name}"/>
                                            </HierarchicalDataTemplate>
                                            <HierarchicalDataTemplate DataType="{x:Type utility:ListPropertyValue}">
                                                <TreeViewItem Header="{Binding Path=Name}" IsExpanded="True">
                                                    <ListView Margin="0" ItemsSource="{Binding Value}">
                                                        <ListView.View>
                                                            <Binding Path="View" Mode="OneWay">
                                                                <Binding.Converter>
                                                                    <converter:GridViewColumnRemoveConverter />
                                                                </Binding.Converter>
                                                                <Binding.ConverterParameter>
                                                                    <x:Array Type="{x:Type system:String}">
                                                                        <system:String>SimulationMode</system:String>
                                                                        <system:String>Index</system:String>
                                                                        <system:String>IsInverse</system:String>
                                                                        <system:String>CorrectionValue</system:String>
                                                                        <system:String>IgnoreInterlock</system:String>
                                                                        <system:String>RangeList</system:String>
                                                                    </x:Array>
                                                                </Binding.ConverterParameter>
                                                            </Binding>
                                                        </ListView.View>
                                                        <ListView.ItemContainerStyle>
                                                            <Style TargetType="ListViewItem">
                                                                <Setter Property="Control.HorizontalContentAlignment" Value="Stretch" />
                                                            </Style>
                                                        </ListView.ItemContainerStyle>
                                                    </ListView>
                                                </TreeViewItem>
                                            </HierarchicalDataTemplate>
                                        </TreeView.Resources>
                                        <TreeView.ItemContainerStyle>
                                            <Style TargetType="TreeViewItem">
                                                <Setter Property="TreeViewItem.IsExpanded" Value="True"/>
                                            </Style>
                                        </TreeView.ItemContainerStyle>
                                    </TreeView>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="{DynamicResource ResourceKey=Operation}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="{DynamicResource ResourceKey=Operation}" Tag="{Binding}" Click="ClickOperation" Padding="5" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
