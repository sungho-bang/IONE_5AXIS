<UserControl x:Class="FAFramework.GUI.Standard.ECCommunicationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             xmlns:converter="clr-namespace:FAFramework.Utility.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.IsEnabled>
            <Binding Path="ReadOnly">
                <Binding.Converter>
                    <converter:BoolInverseConverter />
                </Binding.Converter>
            </Binding>
        </Grid.IsEnabled>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <GroupBox>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="IP" />
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Part.IPAddress}" />
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="PORT" />
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Part.Port}"/>
                </Grid>
            </GroupBox>
        </StackPanel>
        <Grid Grid.Column="1">
            <GroupBox Header="COMMUNICATION TEST">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="Auto" MinHeight="50"/>
                    </Grid.RowDefinitions>
                    <TextBox Grid.Row="0" Text="{Binding Part.ECString, Mode=OneWay}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="WrapWithOverflow" />
                    <Grid Grid.Row="1">
                        <ContentControl Content="{Binding CommandControl}" />
                    </Grid>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
