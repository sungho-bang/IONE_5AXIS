# This repository intentionally tracks all project files.
# Build outputs, binaries, logs, obj/bin folders, packages, images, and DLL/EXE files
# are not ignored because the equipment project needs to be uploaded as a full folder.
#
# Git's internal .git directory is never added by Git, so no ignore rule is needed here.
